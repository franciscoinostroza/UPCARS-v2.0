name: Sync Google Reviews

on:
  schedule:
    - cron: '*/15 * * * *'

jobs:
  reviews:
    runs-on: ubuntu-latest
    steps:
      - name: Sync Reviews
        run: curl -s -f "https://upcars-v2-ka37nuu0f-franciscoinostrozaa-7344s-projects.vercel.app/api/cron/reviews"
